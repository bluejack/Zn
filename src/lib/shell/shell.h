#ifndef ZINC_SHELL
#define ZINC_SHELL
/* TODO: Literify. */

/*
** Zinc Shell
**
** An interactive interpretter that combines the filesystem control of a
** command line shell with the zinc runtime.
**
*/

void run_shell(char **environ);

/* TODO: Quotify. */
#endif
